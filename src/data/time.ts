import moment from "moment";

const dateFormat: string = "YYYY-MM-DD";
const timeFormat: string = "HH:mm:ss";
const dateTimeFormat: string = dateFormat + " " + timeFormat;

export { dateFormat, timeFormat, dateTimeFormat };
